<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="#FFFFFF" /> <!-- Màu nền -->
    <stroke
        android:width="4dp"
        android:color="#320000" /> <!-- Màu viền -->
    <corners android:radius="8dp" /> <!-- Bo góc (nếu cần) -->
    <padding
        android:left="4dp"
        android:top="4dp"
        android:right="4dp"
        android:bottom="4dp" /> <!-- Khoảng cách bên trong (nếu cần) -->
</shape>
